import { UsersProvider, UserContext } from '.'

describe('UsersProvider', () => {
  it('is truthy', () => {
    expect(UsersProvider).toBeTruthy()
  })

  it('has a truthy UserContext', () => {
    expect(UserContext).toBeTruthy()
  })
})
